<%= form_with(model: [:admin,taxon], local: true) do |form| %>
<% if taxon.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(taxon.errors.count, "error") %> prohibited this taxon from being saved:</h2>

  <ul>
    <% taxon.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= form.label :name %>
  <%= form.text_field :name %>
</div>

<div class="field">
  <%= form.label :meta_title %>
  <%= form.text_field :meta_title %>
</div>

<div class="field">
  <%= form.label :meta_title %>
  <%= form.text_field :meta_title %>
</div>

<div class="field">
  <%= form.label :meta_description %>
  <%= form.text_area :meta_description %>
</div>
<div class="actions">
  <%= form.submit %>
</div>

<% end %>